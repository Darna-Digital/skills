# Tailwind

- Prefer default values over arbitrary values.
- Prefer rem over px.
- If a line gets very long with classes, use the cn helper to break it up. Structure the multi lines in a way that makes sense.